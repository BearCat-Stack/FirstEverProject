# Gemini AI SDK
google-generativeai>=0.8.0

# Screen capture
pyautogui>=0.9.54

# Image processing (required by pyautogui and Gemini SDK)
Pillow>=10.0.0

# Global hotkey listener (pynput works without admin on Windows, keyboard as fallback)
pynput>=1.7.0
keyboard>=0.13.5

# Environment variable management
python-dotenv>=1.0.0

# tkinter is included in the Python standard library (no install needed)
# On Ubuntu/Debian, if tkinter is missing, run:
#   sudo apt-get install python3-tk
